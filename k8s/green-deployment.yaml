apiVersion: apps/v1
kind: Deployment
metadata:
  name: pythonapp-green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pythonapp
      version: green
  template:
    metadata:
      labels:
        app: pythonapp
        version: green
    spec:
      containers:
      - name: pythonapp
        image: anirudh3369/myapp:latest
        ports:
        - containerPort: 5000
        livenessProbe:
          httpGet:
            path: /
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 5
